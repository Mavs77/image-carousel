export const navCircles = document.querySelectorAll('.navCircles .circle');
export const leftArrow = document.querySelectorAll('.arrowButton')[0];
export const rightArrow = document.querySelectorAll('.arrowButton')[1];
export const imgContainer = document.getElementById('imgContainer');
